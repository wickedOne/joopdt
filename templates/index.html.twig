{% extends 'base.html.twig' %}

{% block body %}
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
        <header class="masthead mb-auto text-center">
            <div class="inner">
                <h3 class="masthead-brand">{{ 'page.index.title'|trans }}</h3>
            </div>
        </header>
        <main role="main" class="inner cover">
            <p class="lead">{{ 'page.index.paragraph_one'|trans }}</p>
            <p class="lead">{{ 'page.index.paragraph_two'|trans }}</p>
            <p class="lead">{{ 'page.index.paragraph_three'|trans }}</p>
            <p class="lead">
                <a href="{{ url('app_story') }}" class="btn btn-primary float-right mt-5">verhaal toevoegen</a>
            </p>
        </main>

        <footer class="mastfoot mt-auto">
            <div class="inner">

            </div>
        </footer>
    </div>
{% endblock %}